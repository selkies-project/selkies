# Dependencies of the test suites, on top of an installed selkies.
# aiohttp and psutil come with selkies itself.
pytest
# Capture and audio backends the server under test loads
pixelflux
pcmflux
# Browser drivers for the e2e tier; the browsers themselves come from
# `playwright install chromium firefox webkit`
playwright
# Raw WebSocket client for the protocol suites
websockets
# Wayland observer client that proves input reached the compositor seat
pywayland
