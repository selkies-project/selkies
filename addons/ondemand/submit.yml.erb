---
# The basic template runs template/script.sh on the node and reaches its port
# through /rnode/<host>/<port>/; what the scheduler needs beyond the hours and
# the queue is the site's to add here.
batch_connect:
  template: "basic"
